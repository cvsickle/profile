# Build and push Docker image on release
when:
  - event: release
  - event: manual

clone:
  git:
    image: woodpeckerci/plugin-git
    settings:
      depth: 0
      tags: true

steps:
  - name: build-and-push
    image: woodpeckerci/plugin-kaniko:latest
    settings:
      registry: git.cvsickle.com
      repo: cvsickle/profile
      tags:
        - latest
        - ${CI_COMMIT_TAG}
      username: woodpecker
      password:
        from_secret: forgejo_publish_token
      dockerfile: Dockerfile
      context: .
      dry-run: false